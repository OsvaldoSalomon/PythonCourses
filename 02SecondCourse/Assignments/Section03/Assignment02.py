"""
Create a function called separate() that accepts a string as an argument
and return a list containing each of the characters of the string separated
as individual items in the list.
"""


def separate(sentence):
    print(list(sentence))


separate("Hello there")
